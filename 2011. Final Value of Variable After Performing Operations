var finalValueAfterOperations = function(operations) {
    let result = 0;
    for(s of operations) {
        if(s === 'X++' || s === '++X') {
            result += 1;
        } else {
            result -= 1;
        }
    }
    return result;
};
